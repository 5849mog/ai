{
  "name": "gomoku-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python -m http.server 4173",
    "test": "node --test",
    "check": "node --check app.js && node --check engine.js && node --check engine.worker.js"
  }
}
